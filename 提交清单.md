# 提交清单

## 已完成

- [x] `question-1` 项目代码。
- [x] `/v1` 关键词搜索 API 与页面。
- [x] `/v2` 语义搜索 API 与页面。
- [x] `/v3` Agent 对话页面与 API。
- [x] Agent 工具调用过程展示。
- [x] Agent 仅注册 `readFile(fname)` 一个工具。
- [x] HTML 可见正文抽取，排除 `script/style`。
- [x] README 验收脚本：`question-1/scripts/verify.py`。
- [x] 单元测试：`question-1/tests/`。
- [x] 扩展测试：`question-1/test2/`。
- [x] 覆盖率测试：`question-1/scripts/coverage_test2.py`，当前 82.68%。
- [x] CI：`.github/workflows/question-1-ci.yml`。
- [x] 最终说明文档：`docs/final-delivery.md`。
- [x] prompt 截图目录说明：`prompt/README.md`。
- [x] 效果截图目录说明：`screenshot/README.md`。

## 需要候选人本人补充

- [ ] 将 AI 交互提示词截图放入 `prompt/`。
- [ ] 将最终运行效果截图放入 `screenshot/`。
- [ ] 将个人简历放入仓库根目录。
- [ ] 检查并确认 Git 历史完整。
- [ ] 打包整个 Git 仓库，包括 `.git` 目录。

## 建议打包命令

在仓库根目录执行：

```bash
git status --short
git gc
zip -r your-name-exam.zip . -x "node_modules/*" ".venv/*" "__pycache__/*" "*.pyc"
```

注意：题目要求必须以 `.zip` 邮件附件直接提交，不接受网盘链接或 Git 仓库链接。

