# On-Call 助手项目介绍

## 一句话概述

`question-1` 是一个基于部门 On-Call SOP HTML 文档的 Web 应用，提供关键词搜索、语义搜索和 Agent 对话助手三个能力，帮助值班工程师快速定位故障处理流程。

项目入口：

```bash
cd question-1
python3 main.py
```

访问地址：

- `http://127.0.0.1:8000/v1`：关键词搜索
- `http://127.0.0.1:8000/v2`：语义搜索
- `http://127.0.0.1:8000/v3`：On-Call Agent

## 技术方案

项目采用 Python 标准库实现，无需安装第三方依赖。整体设计重点是稳定、可验收、模块解耦和可扩展。

核心技术点：

- HTTP 服务：`http.server.ThreadingHTTPServer`
- HTML 解析：标准库 `html.parser.HTMLParser`
- 关键词检索：可见正文索引 + token / n-gram 评分
- 语义检索：领域概念扩展 + 向量化 token 相似度
- Agent：受控工具调用编排
- 工具系统：插件式 `ToolRegistry`
- LLM 接口：抽象 `LLMClient`，预留 OpenAI / CodeFuse provider
- 报告生成：Markdown / txt 结构化报告
- 自动化验证：`unittest` + 标准库 `trace` 覆盖率统计
- CI/CD：GitHub Actions

项目刻意保持零第三方依赖，这样评审环境只要有 Python 3.9+ 就能运行和验收。

## 项目结构

```text
question-1/
  main.py                 # 应用启动入口
  app/
    server.py             # HTTP server 与路由分发
    api/                  # /v1 /v2 /v3 API 与页面
    core/                 # 配置加载、依赖装配
    documents/            # SOP 文档模型、仓库、HTML parser
    search/               # 搜索插件：keyword / semantic / hybrid
    agent/                # On-Call Agent 编排
    tools/                # Agent 工具，目前仅 readFile
    llm/                  # 通用 LLM 接口与 provider 预留
    reports/              # 验收报告生成
    utils/                # 文本、HTML 等基础工具
  config/
    config.json           # 数据目录、检索参数、语义概念、Agent 配置
  data/
    sop-001.html ...      # SOP HTML 数据
    catalog.json          # Agent 可读取的文档目录索引
  tests/                  # 基础单元测试
  test2/                  # 扩展测试，覆盖率不低于 80%
  scripts/
    verify.py             # README 验收脚本
    coverage_test2.py     # test2 覆盖率脚本
```

## 核心模块说明

### 1. API 层

位置：

```text
question-1/app/api/
question-1/app/server.py
```

职责：

- 接收 HTTP 请求。
- 解析 query string 和 JSON body。
- 调用对应业务服务。
- 返回 JSON 或 HTML 页面。

主要路由：

- `GET /v1`：关键词搜索页面
- `POST /v1/documents`：新增或更新文档
- `GET /v1/search?q=...`：关键词搜索 API
- `GET /v2`：语义搜索页面
- `GET /v2/search?q=...`：语义搜索 API
- `GET /v3`：Agent 对话页面
- `POST /v3/chat`：Agent 对话 API

设计上，API 层只做协议适配，不直接写搜索、解析和 Agent 业务逻辑。

### 2. 文档解析层

位置：

```text
question-1/app/documents/
```

核心文件：

- `models.py`：定义 `Document`
- `repository.py`：加载、保存、索引 SOP 文档
- `parsers/base.py`：文档解析器抽象接口
- `parsers/html_parser.py`：HTML 可见正文解析器

关键能力：

- 提取 HTML `<title>`。
- 提取页面可见正文。
- 排除 `script`、`style`、`noscript`、`template`。
- 自动解码 HTML entity，例如 `&amp;`、`&#45;`、`&#38;`。
- 抽取标题和章节信息。
- 生成 `data/catalog.json`，供 Agent 通过 `readFile("catalog.json")` 合法读取。

这个模块是 Phase 1 能正确通过 `replication` 和 `&` 验收的关键。

### 3. 关键词搜索模块

位置：

```text
question-1/app/search/keyword.py
```

功能：

- 基于 HTML 可见正文建立检索内容。
- 支持英文、数字、符号 token。
- 对中文使用字符 n-gram，提高中文关键词召回。
- 计算简单相关性分数。
- 返回 `id`、`title`、`snippet`、`score`。

典型验收：

- 搜索 `OOM` 返回 `sop-001`。
- 搜索 `故障` 返回多个 SOP。
- 搜索 `replication` 返回空，因为该词只在 script 中。
- 搜索 `CDN` 返回 `sop-003` 和 `sop-010`。
- 搜索 `%26` 返回正文中包含 `&` 的文档。

### 4. 语义搜索模块

位置：

```text
question-1/app/search/semantic.py
question-1/config/config.json
```

功能：

- 不要求查询词在文档中精确出现。
- 通过配置文件中的 `semantic_concepts` 做领域概念扩展。
- 将 query 和 document 扩展后 token 化。
- 使用 cosine 相似度和概念匹配 bonus 排序。

示例：

- `服务器挂了` 会关联后端服务、SRE、Kubernetes、服务不可用等概念。
- `黑客攻击` 会关联安全、入侵、漏洞、DDoS 等概念。
- `机器学习模型出问题` 会关联 AI、推荐、模型、GPU、质量下降等概念。

该实现不依赖外部 embedding 模型，优点是离线稳定；不足是语义泛化能力不如真实 embedding。

### 5. Agent 模块

位置：

```text
question-1/app/agent/
```

核心文件：

- `models.py`：`ToolCall`、`AgentResponse`
- `service.py`：On-Call Agent 编排逻辑

Agent 工作流程：

1. 接收用户问题。
2. 调用唯一工具 `readFile("catalog.json")`。
3. 根据 catalog 中的标题、章节和预览内容定位相关 SOP。
4. 调用 `readFile("sop-xxx.html")` 读取具体 SOP。
5. 解析 SOP 正文。
6. 抽取相关段落并生成回答。
7. 返回答案、工具调用过程和来源文件。

重要约束：

- Agent 只注册一个工具：`readFile`。
- 不能列目录。
- 不能使用通配符。
- 不能读取 `data/` 之外的文件。
- 前端会展示每一次工具调用。

这满足题目对 Phase 3 的核心要求。

### 6. 工具模块

位置：

```text
question-1/app/tools/
```

核心文件：

- `base.py`：工具抽象类
- `read_file.py`：`readFile(fname)` 实现
- `registry.py`：工具注册表

`readFile` 的安全限制：

- 只接受普通文件名。
- 禁止 `/`、`\`、`*`。
- 只能读取 `data/` 目录下的文件。
- 文件不存在时抛出明确错误。

### 7. LLM 抽象层

位置：

```text
question-1/app/llm/
```

核心文件：

- `base.py`：`LLMClient` 抽象接口
- `mock.py`：默认 mock provider
- `openai_provider.py`：OpenAI provider 预留
- `codefuse_provider.py`：CodeFuse provider 预留

当前默认 Agent 不依赖真实 LLM，以保证离线验收稳定。后续如果接入真实模型，只需要实现并切换 provider，不需要改 API、工具或搜索模块。

### 8. 配置与依赖装配

位置：

```text
question-1/app/core/
question-1/config/config.json
```

职责：

- 加载配置。
- 指定 `data_dir`、端口、检索 top-k、语义概念、Agent 最大步骤数等。
- 装配 parser、repository、search provider、tool registry、agent service。

这样可以避免业务模块互相硬编码依赖。

### 9. 报告与测试

位置：

```text
question-1/scripts/
question-1/reports/
question-1/tests/
question-1/test2/
```

测试命令：

```bash
cd question-1
make test
make test2
make coverage-test2
make verify
```

当前结果：

```text
tests: 12 tests OK
test2: 14 tests OK
test2 app coverage: 82.68%
README verification: Overall PASS
```

`scripts/verify.py` 会按照 README 的验收案例生成：

- `reports/verification.md`
- `reports/verification.txt`

## 三个阶段的功能说明

### Phase 1：关键词搜索

用户在 `/v1` 页面输入关键词，系统返回匹配 SOP。

特点：

- 只检索可见正文。
- 支持 HTML entity 解码。
- 支持中英文和符号检索。
- 支持通过 `POST /v1/documents` 添加文档。

### Phase 2：语义搜索

用户在 `/v2` 页面输入自然语言问题，系统返回语义相关 SOP。

特点：

- 查询词不需要精确出现在文档中。
- 通过领域概念扩展实现离线语义召回。
- 结果按相关性分数排序。

### Phase 3：On-Call Agent

用户在 `/v3` 页面输入 On-Call 问题，Agent 根据 SOP 给出处理建议。

特点：

- 展示工具调用过程。
- 根据问题定位一个或多个 SOP。
- 对 P0 故障可综合多个 SOP。
- 严格遵守唯一工具 `readFile` 的约束。

## 为什么这样设计

本项目的核心目标不是堆技术，而是稳定满足 README 验收，并体现良好的工程质量。

因此采用了以下取舍：

- 使用标准库实现，减少评审环境依赖风险。
- 采用模块化设计，保证 API、解析、搜索、Agent、工具、LLM 抽象分离。
- 使用配置化语义概念，便于后续扩展新的部门 SOP。
- Agent 使用 `catalog.json` 定位文件，既满足不能列目录的约束，也保证可解释性。
- 保留 OpenAI / CodeFuse provider，方便后续升级真实 LLM。

## 可继续优化方向

1. 接入真实中文 embedding 模型，提升语义搜索泛化能力。
2. 将 SOP 按 section/chunk 建立更细粒度索引。
3. 接入真实 LLM，让 Agent 回答更自然、更具综合推理能力。
4. 为 Agent 回答增加明确引用，例如显示段落来源和章节名。
5. 优化前端页面，增加搜索高亮、工具调用时间线、来源折叠。
6. 将用户上传文档和内置 demo 数据拆分到不同目录。
7. CI 增加 Python 3.9、3.10、3.11 矩阵测试。
8. 增加端到端 HTTP 测试，覆盖真实服务启动后的 API 行为。

